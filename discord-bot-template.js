const discord = require("discord.js")
const client = require("discord.js")
const token = "SUPA_SECRET_TOKEN"

client.on("ready") => {
  console.log("Ready!!")
}

client.on("message") => {
  
}

client.login(token)
